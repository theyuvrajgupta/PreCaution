# Shell scripts must keep LF line endings even when checked out on Windows.
# A CRLF-terminated run.sh fails on macOS/Linux with "bad interpreter: ...^M".
*.sh text eol=lf

# PowerShell scripts are Windows-side; keep them CRLF.
*.ps1 text eol=crlf
