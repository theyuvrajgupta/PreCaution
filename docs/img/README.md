# Media for the top-level README

Assets referenced by `../../README.md`:

- **`demo.gif`** — a cold end-to-end run of the locked demo protocol (paste, stream the
  pipeline, get the sourced brief). The animated hero at the top of the README. Re-capture
  by screen-recording a run and converting the clip to an optimized GIF.
- **`hazard-card-azide.png`** — the step-5 sodium azide hazard card, showing its NOAA
  CAMEO citation chip.
- **`interaction-section.png`** — both hazard cards plus the reframed no-data section,
  for the "What it catches" section.

The two PNGs were captured via Playwright against the demo protocol's brief. Re-capture
them by running the app locally and re-shooting the elements (`.hazard-card` #2 and
`.interaction-section`) if the UI changes.
