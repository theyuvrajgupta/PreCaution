# Screenshots for the top-level README

Two images referenced by `../../README.md`, captured via Playwright against the demo
protocol's brief:

- **`hazard-card-azide.png`** — the step-5 sodium azide hazard card, showing its NOAA
  CAMEO citation chip. The README's hero image.
- **`interaction-section.png`** — both hazard cards plus the reframed no-data section,
  for the "What it catches" section.

Re-capture by running the app locally and re-shooting these two elements
(`.hazard-card` #2 and `.interaction-section`) if the UI changes.
